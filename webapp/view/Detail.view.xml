<mvc:View
        controllerName="sap.ui.demo.walkthrough.controller.Detail"
        xmlns="sap.m"
        xmlns:mvc="sap.ui.core.mvc">
    <Page
            title="{i18n>detailPageTitle}"
            showNavButton="true"
            navButtonPress=".onNavBack">
        <ObjectHeader
                responsive="true"
                fullScreenOptimized="true"
                number="{
				parts: [{path: 'ExtendedPrice'}, {path: 'view>/currency'}],
				type: 'sap.ui.model.type.Currency',
			    	formatOptions: {
			    		showMeasure: false
			    	}
			    }"
                numberUnit="{view>/currency}"
                intro="{data>/ShipperName}"
                title="{data>/ProductName}"
        >
            <attributes>
                <ObjectAttribute title="{i18n>quantityTitle}" text="{data>/Quantity}"></ObjectAttribute>
                <ObjectAttribute title="{i18n>dateTitle}" text="{
					path: 'data>/ShippedDate',
					type: 'sap.ui.model.type.Date',
					formatOptions: {
					  style: 'long',
					  source: {
						pattern: 'yyyy-MM-ddTHH:mm:ss'
					  }
					}
				  }"/>
            </attributes>
        </ObjectHeader>
        <footer>
            <OverflowToolbar>
                <ToolbarSpacer/>
                <Button
                   text="Edit"
                   ariaDescribedBy="rejectButtonDescription genericButtonDescription"
                />
                <ToolbarSpacer/>
                <Button
                   type="Reject"
                   text="Delete"
                   press=".onDelete"
                   ariaDescribedBy="rejectButtonDescription genericButtonDescription"
                />
                <ToolbarSpacer/>
            </OverflowToolbar>
        </footer>
    </Page>
</mvc:View>
